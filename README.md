# SFD-Net

To run the codebase, you just need to execute two scripts from this directory in order.

First, generate the required saliency maps by running:
```bash
python generate_saliency.py
```

Once that finishes, you can start the training process:
```bash
python train.py
```
